import React from "react";
import { useRouter } from "next/dist/client/router";

const Join = () => {
	const router = useRouter();
   // RETURN
	return (
		<>
			<section>
				<h1>회원가입</h1>
			</section>
		</>
	);
};
export default Join;
